apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-product-service
spec:
  selector:
    app: product-service
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  type: ClusterIP