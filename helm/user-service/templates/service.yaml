apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-user-service
spec:
  selector:
    app: user-service
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  type: ClusterIP