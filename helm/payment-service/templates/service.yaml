apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-payment-service
spec:
  selector:
    app: payment-service
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  type: ClusterIP