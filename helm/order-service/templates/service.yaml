apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-order-service
spec:
  selector:
    app: order-service
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  type: ClusterIP